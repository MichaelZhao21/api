module.exports = () => {return (new Date()).toISOString()}

// TODO: Change to log function, append date to message and console log
// or actually add a function to do that
// Also move these to funcs folder